# SosoBook 體重競賽 App｜MVP 需求與技術規格

## 產品定位

SosoBook 是以「手帳 × 校園運動會 × 健身競賽」為世界觀的手機優先 PWA。

核心流程：

> 選人物 → 每天記錄體重 → 選貼紙打卡 → 日曆累積紀錄 → 參加多場競賽 → 排名結算 → 保存競賽證書。

目前專案已有的素材資料夾：

- `public/img/avatar/`：人物角色
- `public/img/nav/`：主要導覽 icon
- `public/img/sticker/`：日記貼紙
- `public/img/reference/spring-journal-reference.webp`：手繪日記本與校園生活參考圖

## MVP 目標

第一版以真實多人使用為目標，完成 Google 登入、雲端資料同步、每日打卡、競賽邀請、排行榜與結算證書的端到端流程。

第一版不包含：

- 團體賽
- 推播通知
- 目標體重與目標進度
- 公開 Profile
- 貼紙解鎖與成就貼紙
- App 內管理者按鈕
- Render 或自建 API 後端

## 技術與部署

- Vue 3、Vite、JavaScript
- Pinia、Vue Router
- Tailwind CSS 加客製手帳元件
- Firebase Authentication 的 Google 登入
- Cloud Firestore
- `vite-plugin-pwa`
- GitHub Pages 靜態部署
- GitHub Actions 加 Firebase Admin SDK 建立競賽證書
- 日期與日界線以 `Asia/Taipei` 為準

GitHub Actions 每日執行一次，並保留手動 `workflow_dispatch` 補跑。結算工作不保存大型 artifact 或 cache。

## 整體視覺

網站底圖採淺色紙張與低對比格線，營造可書寫的日記本空間。格線以藍灰色細線呈現，間距約 24px 至 28px，避免變成工程方格或技術儀表板。日曆、登入與角色選擇頁可使用較明顯的格線，體重輸入、排行榜與證書頁則降低格線對比，確保數字與主要操作清楚。

底圖不使用全螢幕玻璃效果、霓虹線條或動態網格。貼紙、人物與手繪插圖負責提供視覺焦點，格線只作為紙張材質與版面節奏。

## 使用者與健身卡

首次 Google 登入後設定：

- 暱稱
- 身高（cm）
- 自選人物角色

健身卡是個人資料設定頁，顯示：

- 最新體重
- BMI：`最新體重 kg / 身高 m²`
- 連續打卡天數
- 競賽歷史與冠軍紀錄

角色可隨時更換，不影響歷史成績。健身卡在 MVP 只供本人查看。

## 每日體重日記

每個日期最多一筆體重紀錄，使用 `YYYY-MM-DD` 作為日期識別。

每筆紀錄包含日期、體重 kg、貼紙 ID、資料來源、建立時間與修改時間。

規則：

- 今日可直接打卡
- 不允許記錄未來日期
- 可修改任何歷史日期
- 可在該日紀錄的編輯模式刪除；刪除後若要重新建立，仍遵循今日打卡與最近 7 天補登規則
- 缺少日期時，只能補登最近 7 天
- 補登資料會計入日曆、連續打卡與競賽計算，並標示為補登
- 現有全部貼紙均開放
- 修改體重時可以同步修改貼紙

日曆顯示月曆、貼紙、連續打卡天數。點擊日期後顯示當日體重、貼紙、補登狀態與和前一次紀錄的變化。

## 競賽規則

MVP 只支援個人賽。每場競賽包含名稱、開始日期、結束日期、房主、參賽者、6 位大寫英文字母邀請碼、暫算排行榜與結算證書。

加入規則：

- 使用者透過邀請碼加入
- 競賽開始日的體重是每位參賽者的基準
- 使用者可以建立或加入尚未結束的競賽，不因當下沒有體重資料而阻擋
- 缺少開始日體重時，加入後顯示待補基準提示；尚未取得基準前不列入暫算名次
- 開始日仍在最近 7 天內時，可從提示前往補登開始日資料
- 開始日超過 7 天時仍可保留參賽紀錄，但無法補登開始日，暫不列入排名
- 不要求每天都有體重資料；未來另以填寫率／合格率評估參賽完整度
- 開始前可以自行退出
- 開始後不可自行退出
- 房主可管理競賽名稱；開始與結束日期只能在開賽前調整
- 參賽者與房主只能在開賽前退出或移除成員，開賽後名單鎖定

排名公式：

```text
減重百分比 = (開始日體重 - 目前採用體重) / 開始日體重 × 100
```

正值代表減重，負值代表增重。同分採並列名次。結束日沒有紀錄時，使用結束日前最近一筆紀錄。

## 暫算排行榜與官方證書

競賽進行中，使用者完成打卡後由前端計算自己的暫算減重百分比與期間曲線，寫入競賽成員的公開資料。公開資料只包含暱稱、角色、減重百分比、暫算名次、期間百分比曲線與最後更新時間，不包含實際體重。大家模式顯示參賽者的減重百分比曲線；個人模式由用戶端使用本人原始體重紀錄顯示實際體重曲線，實際體重不寫入競賽公開資料。

競賽結束後，GitHub Actions 使用 Firebase Admin SDK 讀取原始體重資料，建立不可變的證書快照，保存：

- 競賽日期與規則版本
- 每位參賽者的開始體重
- 結束採用體重與日期
- 減重百分比
- 名次
- 打卡天數
- 競賽期間曲線資料

證書可由該場所有參賽者查看，但其他人的曲線只顯示百分比，本人可查看自己的實際體重曲線。證書建立後，日後修改個人日記不影響歷史結果。

已結束競賽不再出現在進行中列表，改由競賽歷史查看。結算腳本必須具備冪等性，重跑不能改寫已鎖定證書。

## 建議 Firestore 結構

```text
users/{uid}
users/{uid}/weightRecords/{dateKey}

competitions/{competitionId}
competitions/{competitionId}/members/{uid}
competitions/{competitionId}/certificate
competitions/{competitionId}/certificateParticipants/{uid}

competitionInviteCodes/{inviteCode}
```

安全邊界：

- 使用者資料與原始體重紀錄只有本人可讀寫
- 競賽成員只能讀取該場競賽的公開成員資料
- 暫算成績只能由對應使用者更新自己的公開欄位
- 證書只能由該場參賽者讀取
- 官方證書與 `settled` 狀態不可由用戶端寫入
- Firebase Admin SDK 憑證只放在 GitHub Actions Secrets

## 主要畫面

主要導覽：

```text
日曆｜今日打卡｜競賽｜健身卡
```

必要畫面：Google 登入與首次設定、日曆總覽、今日打卡、日期詳情、競賽列表、建立競賽、邀請碼加入、競賽詳情、結算證書、競賽歷史、健身卡與個人設定。

## 離線與同步

- PWA 可離線開啟 App Shell 與素材
- Firestore 啟用離線持久化
- 離線打卡排入同步佇列
- 恢復連線後自動同步
- 同一天多裝置衝突採最後修改覆蓋
- 同步失敗時顯示待同步或失敗狀態

## 驗收重點

- Google 登入後可建立暱稱、身高與人物角色
- 可新增、修改、補登體重與貼紙
- 日曆可切換月份並顯示貼紙與連續打卡
- 可建立、加入與退出競賽
- 競賽中不公開實際體重，只公開暫算百分比與排名
- GitHub Actions 可每日與手動建立官方證書
- 證書保存競賽期間快照，後續修改日記不影響結果
- 參賽者可查看證書，只有本人可查看實際體重曲線
- GitHub Pages 重新整理子頁仍可正常開啟
- 離線新增紀錄後恢復網路能完成同步
- Firestore Rules 阻止使用者讀取他人的原始體重資料

## 測試重點

減重百分比、同分排名、結束日缺卡、補登 7 天邊界、連續打卡、BMI、邀請碼、證書冪等性、Firestore 權限、離線同步與 GitHub Pages 子路由重新整理。
